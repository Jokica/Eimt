<ul class="navbar-nav">
    @if (User.Identity.IsAuthenticated)
    {
        <li class="nav-item" id="notification-bell">
            <span id="notification-number"></span>
            <i class="fas fa-bell pt-2 pr-2"  style="font-size:1.2em;"></i>
        </li>
        <li class="dropdown">
            <a class="btn btn-info dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user-circle"></i>
            </a>

            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                <a class="dropdown-item" asp-controller="User" asp-action="Profile">Profile</a>
                <form class="form-inline dropdown-item" asp-controller="User" asp-action="logout">
                    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form>
            </div>
        </li>

    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-white" asp-controller="User" asp-action="Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white" asp-controller="User" asp-action="Login">Login</a>
        </li>
    }
</ul>